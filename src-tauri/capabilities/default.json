{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: core APIs and custom tunnel/keyring commands",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "allow-app-commands"
  ]
}
